# Core Django
Django==4.2.17
djangorestframework==3.15.2

# Authentication
djangorestframework-simplejwt==5.3.1

# Filtering
django-filter==24.3

# Database adapter for PostgreSQL (Railway production)
psycopg2-binary==2.9.10

# Database URL parser (works without psycopg2 in SQLite dev mode)
dj-database-url==2.2.0

# PostgreSQL & environment
python-decouple==3.8

# Static files (Railway/Heroku)
whitenoise==6.7.0
gunicorn==22.0.0

# CORS
django-cors-headers==4.4.0

# Payments
stripe==10.6.0

# Image handling — use latest for Python 3.14 compatibility
Pillow>=11.0.0

# Utilities
python-slugify==8.0.4

# Nested routers for variants
drf-nested-routers==0.94.1
